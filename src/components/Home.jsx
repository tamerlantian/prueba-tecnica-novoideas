import { Typography } from "@mui/material";
import React from "react";

const Home = () => {
  return (
    <div>
      <Typography variant="h4" sx={{ fontWeight: "bold" }}>
        Dashboard
      </Typography>
    </div>
  );
};

export default Home;
